<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokédex</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Press Start 2P', cursive;
        }
    </style>
</head>

<body class="bg-blue-300 flex justify-center items-center h-screen">
    <div class="bg-red-600 w-96 h-[550px] rounded-lg p-4 border-4 border-black relative shadow-xl">
        <div class="flex justify-between items-center">
            <div class="w-10 h-10 bg-blue-400 border-2 border-white rounded-full"></div>
            <div class="flex space-x-2">
                <div class="w-4 h-4 bg-green-400 border-2 border-white rounded-full"></div>
                <div class="w-4 h-4 bg-yellow-400 border-2 border-white rounded-full"></div>
                <div class="w-4 h-4 bg-red-400 border-2 border-white rounded-full"></div>
            </div>
        </div>
        <div class="bg-gray-100 w-full h-52 mt-4 rounded-lg flex justify-center items-center border-2 border-black">
            <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>" class="w-32 h-32">
        </div>
        <h2 class="text-center text-2xl font-bold mt-3 uppercase text-white">#<%= pokemon.id %> - <%= pokemon.name %>
        </h2>
        <div class="bg-gray-200 p-2 mt-3 rounded-lg text-black border-2 border-black">
            <p class="text-lg font-semibold">Height: <%= pokemon.height %>
            </p>
            <p class="text-lg font-semibold">Weight: <%= pokemon.weight %>
            </p>
            <p class="text-lg font-semibold">Type: <%= pokemon.types.map(t=> t.type.name).join(', ') %></p>
        </div>
        <div class="text-center mt-5">
            <a href="/" class="px-4 py-2 bg-black text-white rounded-lg">Back</a>
        </div>
    </div>
</body>

</html>